import matplotlib.pyplot as plt

# The following plot line is equivalent to:
#    plt.plot([0, 1, 2, 3], [1, 2, 3, 4])
# where [0, 1, 2, 3] represents x values
# and [1, 2, 3, 4] represents y values

plt.plot([1, 2, 3, 4])
#plt.plot([0, 1, 2, 3], [1, 2, 3, 4])

plt.ylabel('some numbers')
plt.show()
